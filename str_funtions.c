/**
 * _strlen - return the length the a string
 * @s: is the pointer the variable "n"
 * Return: Always 1 (Success)
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != 0; i++)
	{
	}
	return (i);
}
